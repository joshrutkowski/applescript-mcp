/**
 * @jest-environment node
 */

// A simple test file to verify Jest is working correctly
describe('Simple Test', () => {
  test('true should be true', () => {
    expect(true).toBe(true);
  });
});